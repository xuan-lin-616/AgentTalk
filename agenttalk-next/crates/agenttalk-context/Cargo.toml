[package]
name = "agenttalk-context"
version = "0.1.0"
edition = "2021"

[dependencies]
agenttalk-domain = { path = "../agenttalk-domain" }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
